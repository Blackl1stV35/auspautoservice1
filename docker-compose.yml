services:
  metabase:
    image: metabase/metabase:latest
    container_name: metabase
    ports:
      - "3001:3000"
    restart: unless-stopped
    networks:
      - metabase-network

networks:
  metabase-network:
    driver: bridge